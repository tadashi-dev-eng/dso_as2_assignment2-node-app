{
  "name": "todo-app",
  "version": "1.0.0",
  "description": "Simple Node.js To-Do List App for DSO101 Assignment 2",
  "main": "todo.js",
  "scripts": {
    "test": "jest --ci --reporters=default --reporters=jest-junit",
    "build": "echo 'Build step complete'"
  },
  "devDependencies": {
    "jest": "^29.0.0",
    "jest-junit": "^16.0.0"
  },
  "jest": {
    "testEnvironment": "node"
  }
}
